{
  "name": "@canopy-mvp/payments-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "eslint src/"
  },
  "dependencies": {
    "fastify": "^4.26.0",
    "prisma": "^5.9.0",
    "stripe": "^14.0.0",
    "zod": "^3.22.0"
  }
}