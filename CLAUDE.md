# CLAUDE.md
<!-- Generated by Canopy v7 — do not edit manually -->
<!-- Source: canopy-mvp → backend → payments-api -->
<!-- Hash: d28e0a086ea0e2139de057cdd59ff9417bff90e9be082a6591013e6d448860f8 | Generated: 2026-03-31T15:19:02.109Z -->

## Security & Compliance

> ⚠️ LOCKED — These rules cannot be overridden.

### Injection Prevention

#### Parameterized queries only
NEVER concatenate user input into SQL/NoSQL/LDAP queries. ALWAYS use parameterized queries (`$1`, `?`, `:param`), prepared statements, or ORM bindings. Flag: `query("SELECT * FROM users WHERE id = " + id)`, f-strings in SQL, `${variable}` in queries. Use instead: `query("SELECT * FROM users WHERE id = $1", [id])`.
<!-- Source: canopy-mvp (org) -->

### Data Privacy & PII

#### Never disable TLS/SSL verification
NEVER set `rejectUnauthorized: false`, `verify=False`, `InsecureSkipVerify: true`, or `NODE_TLS_REJECT_UNAUTHORIZED=0` in HTTP clients, database connections, or API calls. Flag as CRITICAL if found. Use proper certificate chains; for local dev, use mkcert to create locally-trusted certificates instead.
<!-- Source: canopy-mvp (org) -->

#### Encrypt sensitive data at rest and in transit
MUST use TLS 1.2+ for all network communication and AES-256+ for data at rest. NEVER store passwords in plaintext or use MD5/SHA for hashing; ALWAYS use bcrypt, scrypt, or argon2. NEVER implement custom crypto—use only established libraries (crypto, libsodium, openssl).
<!-- Source: canopy-mvp (org) -->

### Authorization & Access Control

#### Authorization on every endpoint
MUST verify authenticated caller has permission for the specific resource and operation. Every GET/PUT/DELETE /:id route MUST check ownership or role before executing. NEVER rely on client-side guards alone. Example: Check `if (user.id !== resource.ownerId && !user.isAdmin) return 403` on object routes.
<!-- Source: canopy-mvp (org) -->

### Destructive Operations

#### No unguarded destructive operations
NEVER execute DELETE, DROP, rm -rf, git push --force, or git reset --hard without explicit user confirmation. MUST implement soft deletes, provide rollback scripts for migrations, and require confirmation flags for all destructive operations. Example: Use UPDATE is_deleted=true instead of DELETE.
<!-- Source: canopy-mvp (org) -->

## Architecture & Patterns

### Use designated framework
Use designated framework — enforce this standard across all code and AI-generated output.
<!-- Source: canopy-mvp (org) -->

### Structured error handling
Structured error handling — enforce this standard across all code and AI-generated output.
<!-- Source: canopy-mvp (org) -->

### Structured logging
Structured logging — enforce this standard across all code and AI-generated output.
<!-- Source: canopy-mvp (org) -->

### Input validation on every endpoint
Input validation on every endpoint — enforce this standard across all code and AI-generated output.
<!-- Source: canopy-mvp (org) -->

### API versioning
API versioning — enforce this standard across all code and AI-generated output.
<!-- Source: canopy-mvp (org) -->

### Unit test coverage
Unit test coverage — enforce this standard across all code and AI-generated output.
<!-- Source: canopy-mvp (org) -->

### Code organization standards
Code organization standards — enforce this standard across all code and AI-generated output.
<!-- Source: canopy-mvp (org) -->

## Knowledge & Context

### Domain vocabulary
Domain vocabulary — enforce this standard across all code and AI-generated output.
<!-- Source: canopy-mvp (org) -->

### System architecture map
System architecture map — enforce this standard across all code and AI-generated output.
<!-- Source: canopy-mvp (org) -->

### Workflow documentation
Workflow documentation — enforce this standard across all code and AI-generated output.
<!-- Source: canopy-mvp (org) -->

### Decision records
Decision records — enforce this standard across all code and AI-generated output.
<!-- Source: canopy-mvp (org) -->

### Onboarding guide
Onboarding guide — enforce this standard across all code and AI-generated output.
<!-- Source: canopy-mvp (org) -->

### Temporal context
Temporal context — enforce this standard across all code and AI-generated output.
<!-- Source: canopy-mvp (org) -->

---
_Generated by [Canopy](https://canopy.dev). Report issues: [feedback](https://canopy.dev/feedback)_
